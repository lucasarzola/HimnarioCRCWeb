{
  "type": "module",
  "scripts": {
    "consolidar-correcciones": "node tools/apply-firebase-corrections.mjs",
    "deploy": "firebase deploy",
    "deploy:staging": "firebase hosting:channel:deploy staging --expires 30d",
    "deploy:prod": "firebase deploy"
  }
}
